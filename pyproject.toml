[project]
name = "genvideo"
version = "0.1.0"
description = "Web app for memory-bounded MiniMax H3 audio-video generation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.0",
    "psutil>=7.2.2",
]
